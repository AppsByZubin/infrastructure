{{- if .Values.pvc.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.pvc.claimName }}
spec:
  accessModes:
    - {{ .Values.pvc.accessMode | default "ReadWriteOncePod" }}
  resources:
    requests:
      storage: {{ .Values.pvc.size | default "20Gi" }}
{{- end }}
