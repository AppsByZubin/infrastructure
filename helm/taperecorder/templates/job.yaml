apiVersion: batch/v1
kind: Job
metadata:
  name: taperecorder
  labels:
    app: taperecorder
  # FIXED: Moved 'annotations' to be a sibling of 'labels', not inside it
  annotations:
    argocd.argoproj.io/sync-options: Replace=true
spec:
  # ttlSecondsAfterFinished: 86400
  template:
    metadata:
      labels:
        app: taperecorder
    spec:
      containers:
      - name: taperecorder
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}

        env:
        {{- range $k, $v := .Values.env }}
        - name: {{ $k }}
          # FIXED: 'value' is now perfectly aligned with 'name' (same vertical line)
          value: "{{ $v }}"
        {{- end }}

        envFrom:
        - secretRef:
            name: taperecorder-secrets

        volumeMounts:
        - name: data
          mountPath: /app/data

        resources:
{{ toYaml .Values.resources | indent 10 }}

      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: taperecorder-data

      restartPolicy: OnFailure